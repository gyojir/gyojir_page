<h1>Editing article</h1>

<%= render 'form' %>

<h2>Add a tags:</h2>
<!-- mangas/#id/tags  -->
<%= form_for([@manga, @manga.tags.build]) do |f| %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', mangas_path %>
